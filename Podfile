# Comment the next line if you're not using Swift and don't want to use dynamic frameworks
use_frameworks!

target 'Ranalyse' do
  platform :ios, '11.0'

  # Pods for Ranalyse
  pod 'CoreGPX', '= 0.5.1'
  
end

target 'Ranalyse WatchKit App' do
  platform :watchos, '4.0'

  # Pods for Ranalyse WatchKit App

end

target 'Ranalyse WatchKit Extension' do
    platform :watchos, '4.0'

  # Pods for Ranalyse WatchKit Extension

end
